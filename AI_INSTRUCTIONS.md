# AI Instructions

The canonical shared working procedure is:

- `C:\Development\_shared\website-operating-procedure.md`

This repo also keeps a project copy in:

- `docs/operating-procedure.md`

All AI assistants should read the global SOP before making workflow, Git, deployment, or release decisions. Tool-specific entrypoints also point here:

- `AGENTS.md` for Codex/OpenAI-style agents
- `CLAUDE.md` for Claude
- `.github/copilot-instructions.md` for GitHub Copilot

If an AI tool does not automatically load these files, say: "Read the global SOP and follow it for this project." The global SOP defines what that instruction means.
